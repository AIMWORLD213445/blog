import { postCount } from 'blog/helpers/post-count';
import { module, test } from 'qunit';

module('Unit | Helper | post count');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = postCount([42]);
  assert.ok(result);
});
