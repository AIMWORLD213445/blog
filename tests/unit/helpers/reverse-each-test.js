import { reverseEach } from 'blog/helpers/reverse-each';
import { module, test } from 'qunit';

module('Unit | Helper | reverse each');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = reverseEach([42]);
  assert.ok(result);
});
